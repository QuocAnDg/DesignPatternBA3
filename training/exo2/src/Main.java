import java.io.IOException;
import java.util.ArrayList;

public class Main {

  public static void main(String[] args) throws IOException {
    ListerMots listerMots = new ListerMots(args[0], new CommenceParStrategy('t'));
    listerMots.imprimerSi();
    ArrayList<Strategy> strategies = new ArrayList<>();
    strategies.add(new CommenceParStrategy('t'));
    str
    Ou ou1 = new Ou(new CommenceParStrategy('t'));
  }
}